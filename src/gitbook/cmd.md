# cmd

## Description

- firse use nvm to change node version to 10
  - run
  ```bash
  nvm use 10
  ```

### cmd

- run local server
  ```bash
    gitbook serve
  ```
- build
  ```bash
    find . -name "*.md" | entr gitbook build
  ```

