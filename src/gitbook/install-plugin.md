# install gitbook plugin step

- add plugin in book.json

```json
    "plugins":[
        "chapter-fold"
    ]
```

- switch node to version 6 use nvm

```bash
nvm use 6
```

- install use gitbook cli

```bash
gitbook install
```


